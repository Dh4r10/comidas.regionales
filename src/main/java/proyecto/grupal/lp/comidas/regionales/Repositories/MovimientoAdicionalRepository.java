package proyecto.grupal.lp.comidas.regionales.Repositories;


import org.springframework.data.jpa.repository.JpaRepository;
import proyecto.grupal.lp.comidas.regionales.Entities.MovimientoAdicional;

public interface MovimientoAdicionalRepository extends JpaRepository<MovimientoAdicional,Long> {
}
